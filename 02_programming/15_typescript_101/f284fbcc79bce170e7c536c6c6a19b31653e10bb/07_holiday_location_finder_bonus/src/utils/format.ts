import { continents, cities } from "../data/data";
sanitizeUserInput("hello", "hello");
function sanitizeUserInput(continents: string, cities: string): void {
  console.log(continents);
  console.log(cities);
  // Write your code here
}

// Leave the line below for tests to work properly.
export { sanitizeUserInput };

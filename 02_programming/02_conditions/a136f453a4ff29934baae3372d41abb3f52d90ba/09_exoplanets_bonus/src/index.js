/ Don't modify these lines for tests to work properly.
const star = require("./data/starData");
const planet = require("./data/exoplanetData");

// Your code here:
let canHabitateLife;

// Don't modifiy this line for test to work properly.
module.exports = canHabitateLife;

const b = {
  orbitalPeriod: 1.51,
  distanceToStar: 0.0115,
  planetRadius: 1.12,
  planetMass: 1.02,
  planetDensity: 0.73, 
  surfaceGravity: 0.81,
};
const c = {
  orbitalPeriod: 2.42,
  distanceToStar: 0.0158,
  planetRadius: 1.10,
  planetMass: 1.16,
  planetDensity: 0.88,
  surfaceGravity: 0.96,
};
module.exports = {
  mass: 0.77,
  radius: 0.91,
  rotationStability: true,
  habitalZone: true,
};
module.exports = {
  spectralClass: "M",
  temperature: 2550,
  mass: 0.089,
};

//Can this planet be home for life ? 
let planetRadius >= 1 && planetRadius <= 2.5 ? true : false;
let planetMass >= 0.32 ? true : false; 

